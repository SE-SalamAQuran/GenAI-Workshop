def mock_llm(prompt: str) -> str:
    """
    This simulates an LLM response.
    Replace this later with:
    - Ollama
    - OpenAI
    - Any other provider
    """

    return (
        "• This text discusses an important topic\n"
        "• It highlights key ideas clearly\n"
        "• The main goal is understanding, not memorization"
    )
